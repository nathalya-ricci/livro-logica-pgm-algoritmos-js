let num = Number(prompt('Informe um número: '));

let anterior = num - 1;
let posterior = num + 1;

alert(`O número anterior a ${num} é ${anterior}.`);
alert(`O número posterior a ${num} é ${posterior}`);
